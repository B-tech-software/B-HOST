"""Route blueprints for the B-raise Flask backend.

This package groups all HTTP route handlers (endpoints) as Flask blueprints.
Keeping routes in their own package makes the app structure easier to
understand and maintain.
"""
